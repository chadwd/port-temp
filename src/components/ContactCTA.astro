---
// Contact call-to-action section for bottom of pages
---

<section class="contact-cta">
  <h2>Let's Connect</h2>
  <p>Interested in working together or have a question?</p>
  <div class="cta-links">
    <a href="mailto:chad.dunbar@gmail.com" class="cta-email">chad.dunbar@gmail.com</a>
    <div class="cta-social">
      <a href="https://linkedin.com/in/chaddunbar" target="_blank" rel="noopener noreferrer">LinkedIn</a>
      <span class="separator">·</span>
      <a href="https://github.com/chadwd" target="_blank" rel="noopener noreferrer">GitHub</a>
    </div>
  </div>
</section>

<style>
  .contact-cta {
    margin-top: var(--space-16);
    padding: var(--space-12) 0;
    border-top: 1px solid var(--color-border);
    text-align: center;
  }

  .contact-cta h2 {
    font-size: var(--text-2xl);
    margin-bottom: var(--space-3);
  }

  .contact-cta > p {
    color: var(--color-text-muted);
    margin-bottom: var(--space-6);
  }

  .cta-links {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-4);
  }

  .cta-email {
    font-size: var(--text-xl);
    font-weight: var(--font-medium);
    color: var(--color-text);
    text-decoration: none;
    border-bottom: 2px solid var(--color-accent);
    transition: all var(--transition-fast);
  }

  .cta-email:hover {
    color: var(--color-accent);
    border-bottom-color: var(--color-text);
  }

  .cta-social {
    display: flex;
    align-items: center;
    gap: var(--space-3);
  }

  .cta-social a {
    color: var(--color-text-muted);
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  .cta-social a:hover {
    color: var(--color-accent);
  }

  .separator {
    color: var(--color-border);
  }
</style>
