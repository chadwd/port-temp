---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Contact"
  description="Get in touch to discuss projects, collaborations, or opportunities."
>
  <div class="container">
    <header class="contact-header">
      <h1>Get in Touch</h1>
      <p class="intro">
        Interested in working together or have a question? Feel free to reach out.
      </p>
    </header>

    <div class="contact-content">
      <div class="contact-method">
        <p>Email me at:</p>
        <a href="mailto:chad.dunbar@gmail.com" class="email-link">chad.dunbar@gmail.com</a>
      </div>

      <div class="social-links">
        <p>Or connect on:</p>
        <ul>
          <li><a href="https://linkedin.com/in/chaddunbar" target="_blank" rel="noopener noreferrer">LinkedIn</a></li>
          <li><a href="https://github.com/chadwd" target="_blank" rel="noopener noreferrer">GitHub</a></li>
        </ul>
      </div>
    </div>
  </div>
</BaseLayout>

<style>
  .contact-header {
    margin-bottom: var(--space-12);
  }

  .intro {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
    max-width: var(--max-width-content);
    margin-bottom: 0;
  }

  .contact-content {
    max-width: var(--max-width-content);
  }

  .contact-method {
    margin-bottom: var(--space-12);
  }

  .contact-method p {
    margin-bottom: var(--space-3);
    color: var(--color-text-muted);
  }

  .email-link {
    font-size: var(--text-2xl);
    font-weight: var(--font-medium);
    color: var(--color-text);
    text-decoration: none;
    display: inline-block;
    border-bottom: 2px solid var(--color-accent);
    transition: all var(--transition-fast);
  }

  .email-link:hover {
    color: var(--color-accent);
    border-bottom-color: var(--color-text);
  }

  .social-links p {
    margin-bottom: var(--space-3);
    color: var(--color-text-muted);
  }

  .social-links ul {
    list-style: none;
    padding: 0;
    display: flex;
    gap: var(--space-6);
  }

  .social-links a {
    font-size: var(--text-lg);
  }
</style>
