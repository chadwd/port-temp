---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import CaseStudyCard from '../components/CaseStudyCard.astro';
import ResponsiveImage from '../components/ResponsiveImage.astro';
import ContactCTA from '../components/ContactCTA.astro';

const allCaseStudies = await getCollection('case-studies');
const featuredCaseStudies = allCaseStudies
  .filter((caseStudy) => caseStudy.data.featured === true)
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
  .slice(0, 3);

const allWriting = await getCollection('writing');
const recentWriting = allWriting
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
  .slice(0, 3);
---

<BaseLayout title="Home">
  <div class="container">
    <section class="hero">
      <h1>Chad Dunbar</h1>
      <p class="hero-subtitle">Principal Product Designer, Platforms and AI Workflows</p>
      <p class="hero-intro">
        15+ years leading enterprise UX.<br />
        Building design systems at scale and shipping AI-driven products.
      </p>
      <a href="/files/resume.pdf" class="resume-button" target="_blank" rel="noopener noreferrer">
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <path d="M11 3H17V9M17 3L9 11M7 5H5C3.89543 5 3 5.89543 3 7V15C3 16.1046 3.89543 17 5 17H13C14.1046 17 15 16.1046 15 15V13" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        View Resume
      </a>
    </section>

    <section class="featured-work">
      <h2>Selected Work</h2>
      {featuredCaseStudies.length === 0 ? (
        <p class="placeholder-text">Case studies coming soon.</p>
      ) : (
        <>
          <div class="case-studies-grid">
            {featuredCaseStudies.map((caseStudy) => (
              <CaseStudyCard
                title={caseStudy.data.title}
                description={caseStudy.data.description}
                slug={caseStudy.slug}
                date={caseStudy.data.date}
                confidentiality={caseStudy.data.confidentiality}
                tags={caseStudy.data.tags}
                coverImage={caseStudy.data.coverImage}
              />
            ))}
            <a href="/work" class="view-all-card">
              <span class="view-all-text">View all work</span>
              <svg class="view-all-arrow" width="24" height="24" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </a>
          </div>
        </>
      )}
    </section>

    <section class="recent-writing">
      <h2>Recent Writing</h2>
      {recentWriting.length === 0 ? (
        <p class="placeholder-text">Writing coming soon.</p>
      ) : (
        <>
          <div class="writing-grid">
            {recentWriting.map((post) => (
              <article class="writing-card">
                <a href={`/writing/${post.slug}`} class="card-link">
                  <div class="card-content">
                    {post.data.coverImage && (
                      <div class="card-image">
                        <img
                          src={post.data.coverImage}
                          alt={post.data.title}
                          loading="lazy"
                          class="cover-image"
                        />
                      </div>
                    )}
                    <h3>{post.data.title}</h3>
                    <p class="description">{post.data.description}</p>
                    <div class="card-footer">
                      <time datetime={post.data.date.toISOString()}>
                        {post.data.date.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}
                      </time>
                      {post.data.tags && post.data.tags.length > 0 && (
                        <ul class="tags">
                          {post.data.tags.map((tag) => (
                            <li class="tag">{tag}</li>
                          ))}
                        </ul>
                      )}
                    </div>
                  </div>
                </a>
              </article>
            ))}
            <a href="/writing" class="view-all-card">
              <span class="view-all-text">View all writing</span>
              <svg class="view-all-arrow" width="24" height="24" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </a>
          </div>
        </>
      )}
    </section>

    <ContactCTA />
  </div>
</BaseLayout>

<style>
  .hero {
    padding-block: var(--space-16);
  }

  .hero h1 {
    font-size: var(--text-5xl);
    margin-bottom: var(--space-2);
  }

  .hero-subtitle {
    font-size: var(--text-3xl);
    color: var(--color-accent);
    font-weight: var(--font-medium);
    margin-bottom: var(--space-3);
  }

  .hero-intro {
    font-size: var(--text-xl);
    color: var(--color-text-muted);
    max-width: var(--max-width-content);
    line-height: var(--leading-relaxed);
    margin-bottom: var(--space-8);
  }

  .resume-button {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-3) var(--space-6);
    background-color: rgba(61, 90, 128, 0.12);
    color: var(--color-accent);
    text-decoration: none;
    border-radius: 9999px;
    font-size: var(--text-base);
    font-weight: var(--font-medium);
    transition: all var(--transition-fast);
    position: relative;
    overflow: hidden;
  }

  .resume-button:hover {
    background-color: rgba(61, 90, 128, 0.18);
    transform: translateY(-1px);
    box-shadow: 0 2px 8px rgba(61, 90, 128, 0.15);
  }

  .resume-button:active {
    transform: translateY(0);
  }

  .resume-button svg {
    flex-shrink: 0;
  }

  .featured-work {
    padding-block: var(--space-16);
  }

  .featured-work h2 {
    margin-bottom: var(--space-8);
  }

  .placeholder-text {
    color: var(--color-text-muted);
    font-style: italic;
  }

  .case-studies-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-6);
    margin-bottom: var(--space-8);
  }

  @media (min-width: 768px) {
    .case-studies-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .view-all-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--space-3);
    background-color: var(--color-surface);
    border: 1px dashed var(--color-border);
    border-radius: 6px;
    padding: var(--space-8);
    text-decoration: none;
    color: var(--color-text-muted);
    transition: all var(--transition-base);
    min-height: 200px;
  }

  .view-all-card:hover {
    border-color: var(--color-accent);
    border-style: solid;
    color: var(--color-accent);
    background-color: var(--color-surface-hover);
    transform: translateY(-2px);
  }

  .view-all-text {
    font-size: var(--text-lg);
    font-weight: var(--font-medium);
  }

  .view-all-arrow {
    transition: transform var(--transition-fast);
  }

  .view-all-card:hover .view-all-arrow {
    transform: translateX(4px);
  }

  .recent-writing {
    padding-block: var(--space-16);
  }

  .recent-writing h2 {
    margin-bottom: var(--space-8);
  }

  .writing-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-6);
    margin-bottom: var(--space-8);
  }

  @media (min-width: 768px) {
    .writing-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .writing-card {
    height: 100%;
  }

  .writing-card .card-link {
    display: block;
    height: 100%;
    text-decoration: none;
    color: inherit;
    background-color: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 6px;
    padding: var(--space-6);
    transition: all var(--transition-base);
  }

  .writing-card .card-link:hover {
    border-color: var(--color-accent);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
    transform: translateY(-2px);
    text-decoration: none;
  }

  .writing-card .card-content {
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  .writing-card .card-image {
    margin-bottom: var(--space-4);
    overflow: hidden;
    border-radius: 6px;
    aspect-ratio: 4 / 3;
    background-color: var(--color-surface-hover);
  }

  .writing-card .card-image :global(.cover-image) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .writing-card h3 {
    font-size: var(--text-2xl);
    font-weight: var(--font-semibold);
    margin-bottom: var(--space-3);
    color: var(--color-text);
  }

  .writing-card .description {
    color: var(--color-text-muted);
    margin-bottom: var(--space-4);
    flex-grow: 1;
  }

  .writing-card .card-footer {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    flex-wrap: wrap;
    margin-top: auto;
  }

  .writing-card .card-footer time {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
  }

  .writing-card .tags {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    gap: var(--space-2);
    flex-wrap: wrap;
  }

  .writing-card .tag {
    background-color: var(--color-surface-hover);
    padding: var(--space-1) var(--space-2);
    border-radius: 4px;
    font-size: var(--text-xs);
    color: var(--color-text-muted);
  }

  @media (max-width: 768px) {
    .writing-card .card-link {
      padding: var(--space-4);
    }

    .writing-card h3 {
      font-size: var(--text-xl);
    }
  }
</style>
