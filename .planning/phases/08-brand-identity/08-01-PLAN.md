---
phase: 08-brand-identity
plan: 1
type: execute
---

<objective>
Implement logo, wordmark, and favicon to replace placeholder branding with actual brand identity.

Purpose: Establish professional brand presence - site currently uses "Portfolio" placeholder text and needs proper brand assets for production readiness.
Output: Brand assets integrated into site header and browser UI (favicon, touch icons).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/layouts/BaseLayout.astro
@src/components/Header.astro

**Prior decisions affecting this phase:**
- Phase 2: System fonts only (no custom fonts added yet - can add later if needed)
- Phase 2: Warm color palette with slate blue accent (#334155)
- Phase 3: Text wordmark approach (currently "Portfolio" placeholder)

**Current state:**
- Header.astro line 21: Uses "Portfolio" as placeholder site name
- BaseLayout.astro line 20: References /favicon.svg (file doesn't exist yet)
- public/ directory exists but contains only CNAME file

**What's needed:**
- Replace "Portfolio" placeholder with actual brand name/wordmark
- Add proper favicon files (multiple sizes for browser/mobile support)
- Maintain existing responsive header behavior
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create brand assets in public directory</name>
  <files>public/favicon.svg, public/favicon.ico, public/apple-touch-icon.png</files>
  <action>
    Create minimal brand assets for the portfolio site:

    1. favicon.svg: Create simple SVG favicon with "CD" monogram or wordmark
       - Use slate blue (#334155) from design system
       - Keep minimal - favicons are tiny (16x16, 32x32)
       - SVG allows scaling for different sizes

    2. favicon.ico: Create 32x32 ICO file for legacy browser support
       - Can generate from SVG or create separate
       - Support 16x16 and 32x32 sizes in single ICO

    3. apple-touch-icon.png: Create 180x180 PNG for iOS home screen
       - Higher resolution needed for Apple devices
       - Use same branding as favicon

    Keep designs minimal and professional - match the "calm, confident" aesthetic from PROJECT.md.
    Use existing color palette (slate blue accent, warm neutrals).
  </action>
  <verify>
    - ls public/ shows favicon.svg, favicon.ico, apple-touch-icon.png
    - file public/favicon.svg shows SVG format
    - file public/favicon.ico shows ICO format
    - file public/apple-touch-icon.png shows PNG format
  </verify>
  <done>Three favicon files created in public/ directory with minimal brand design</done>
</task>

<task type="auto">
  <name>Task 2: Update Header component with brand name</name>
  <files>src/components/Header.astro</files>
  <action>
    Replace the "Portfolio" placeholder text with the actual brand name.

    Based on PROJECT.md context (senior/principal product designer, custom domain chaddunbar.com):
    - Change line 21 from "Portfolio" to "Chad Dunbar" or "CD" (choose based on brevity and design aesthetic)
    - Maintain existing styling (font-size, font-weight, color, hover states)
    - Keep responsive behavior intact (no layout changes)

    Alternative: If the user has a specific wordmark preference, use "Chad Dunbar" as it's more explicit than initials.

    Do NOT add logo images yet - keep text-based wordmark approach from Phase 3 decision.
    Do NOT change any styling - only the text content.
  </action>
  <verify>
    - grep "Chad Dunbar" src/components/Header.astro returns match (or "CD" if that's chosen)
    - grep "Portfolio" src/components/Header.astro returns no matches in site name
    - npm run build succeeds without errors
  </verify>
  <done>Header displays "Chad Dunbar" instead of "Portfolio", build passes</done>
</task>

<task type="auto">
  <name>Task 3: Configure favicon links in base layout</name>
  <files>src/layouts/BaseLayout.astro</files>
  <action>
    Update the favicon references in BaseLayout.astro head section to include all formats:

    Replace line 20 (single favicon.svg) with comprehensive favicon support:
    ```astro
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    ```

    This provides:
    - Modern browsers: favicon.svg (scalable)
    - Legacy browsers: favicon.ico (fallback)
    - iOS devices: apple-touch-icon.png (home screen)

    Keep the existing meta tags and title structure - only modify favicon links.
  </action>
  <verify>
    - grep "favicon.svg" src/layouts/BaseLayout.astro returns match
    - grep "favicon.ico" src/layouts/BaseLayout.astro returns match
    - grep "apple-touch-icon" src/layouts/BaseLayout.astro returns match
    - npm run build succeeds
  </verify>
  <done>BaseLayout.astro includes all three favicon formats, build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Brand identity assets integrated - favicon in browser tabs and "Chad Dunbar" wordmark in header</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000
    3. Check browser tab: Verify favicon appears (may need to hard refresh: Cmd+Shift+R)
    4. Check header: Verify "Chad Dunbar" appears instead of "Portfolio"
    5. Navigate to /work, /about, /contact: Confirm wordmark appears on all pages
    6. Responsive check: Resize browser - confirm header layout still works correctly
    7. Mobile simulation (DevTools): Verify header stacks properly, wordmark visible

    Expected results:
    - Favicon shows in browser tab (CD monogram or design from Task 1)
    - Header shows "Chad Dunbar" (or "CD") in same position/style as "Portfolio" was
    - No layout breaks, no styling issues
    - Responsive behavior unchanged
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] npm run build succeeds without errors
- [ ] All three favicon files exist in public/
- [ ] Header displays brand name instead of "Portfolio"
- [ ] BaseLayout includes all favicon formats
- [ ] Visual verification confirms brand assets display correctly
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Brand name visible in header on all pages
- Favicon displays in browser tabs
- No TypeScript errors or build failures
- Responsive behavior maintained
</success_criteria>

<output>
After completion, create `.planning/phases/08-brand-identity/08-01-SUMMARY.md`:

# Phase 8 Plan 1: Brand Identity Assets Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- `public/favicon.svg` - [Description]
- `public/favicon.ico` - [Description]
- `public/apple-touch-icon.png` - [Description]
- `src/components/Header.astro` - [Description]
- `src/layouts/BaseLayout.astro` - [Description]

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 8 complete. Ready for Phase 9: Real Content.
</output>
