---
phase: 03-core-pages
plan: 01
type: execute
---

<objective>
Create shared layout components: Header with navigation, Footer, and update BaseLayout to include them.

Purpose: Establish the consistent navigation and footer structure that all pages will use.
Output: Reusable Header and Footer components integrated into BaseLayout.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system/02-01-SUMMARY.md
@src/layouts/BaseLayout.astro
@src/styles/global.css

**Prior decisions affecting this phase:**
- Design tokens established in Phase 2 (use CSS custom properties)
- Warm, minimal aesthetic (calm authority, not flashy)
- System fonts for performance
- Content-first approach

**Pages requiring navigation:**
- Home (/)
- About (/about)
- Writing (/writing) - future, but include in nav
- Contact (/contact)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header component with navigation</name>
  <files>src/components/Header.astro</files>
  <action>
    Create a Header component with:

    **Structure:**
    - Site name/logo text link (left) - links to home
    - Navigation links (right) - About, Writing, Contact

    **Styling:**
    - Use .container utility from global.css
    - Sticky header with subtle backdrop blur on scroll (optional, keep simple)
    - Use --color-text, --color-text-muted, --color-border from design tokens
    - Navigation links: subtle, no underline by default, underline on hover
    - Current page indicator (use Astro.url.pathname for active state)
    - Mobile: hamburger menu or simple stacked layout (keep minimal)

    **Accessibility:**
    - Use semantic nav element
    - aria-current="page" on active link
    - Skip link to main content (optional but good practice)

    Keep it minimal and editorial. No logo images, just text. The designer's name or "Portfolio" as wordmark.
  </action>
  <verify>
    - File exists and exports Astro component
    - npm run build succeeds
    - No TypeScript errors
  </verify>
  <done>Header component with navigation links, responsive layout, active state indicator</done>
</task>

<task type="auto">
  <name>Task 2: Create Footer component</name>
  <files>src/components/Footer.astro</files>
  <action>
    Create a simple Footer component with:

    **Structure:**
    - Container with centered content
    - Copyright text with current year (use JS for dynamic year)
    - Optional: social links or email (keep minimal)

    **Styling:**
    - Use .container utility
    - Use --color-text-muted for subdued text
    - Generous top margin (--space-16 or --space-24)
    - Border-top with --color-border for separation
    - Small text (--text-sm)

    Keep extremely minimal. Just copyright and maybe one link. No social media icons unless requested later.
  </action>
  <verify>
    - File exists and exports Astro component
    - npm run build succeeds
  </verify>
  <done>Footer component with copyright, minimal styling, responsive</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Header and Footer into BaseLayout</name>
  <files>src/layouts/BaseLayout.astro</files>
  <action>
    Update BaseLayout to include Header and Footer:

    **Structure:**
    ```
    <body>
      <Header />
      <main>
        <slot />
      </main>
      <Footer />
    </body>
    ```

    **Styling additions to body:**
    - Use flexbox: flex-direction column, min-height 100vh
    - Main gets flex: 1 to push footer to bottom
    - Add padding-top to account for header height

    **Import components:**
    - Import Header from '../components/Header.astro'
    - Import Footer from '../components/Footer.astro'

    Ensure the layout works with the existing index.astro page.
  </action>
  <verify>
    - npm run dev shows header and footer on homepage
    - Footer stays at bottom even with minimal content
    - npm run build succeeds
  </verify>
  <done>BaseLayout includes Header and Footer, flex layout pushes footer to bottom</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Header displays with navigation links
- [ ] Footer displays at bottom of page
- [ ] Navigation links are visible and styled correctly
- [ ] Layout works on mobile viewport (responsive)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Components use design tokens from Phase 2
- Ready for Phase 3 Plan 2 (Core Pages)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-01-SUMMARY.md`:

# Phase 3 Plan 1: Layout Components Summary

**[Substantive one-liner about what was built]**

## Accomplishments
- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified
- `path/to/file` - Description

## Decisions Made
[Key decisions and rationale, or "None"]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Ready for 03-02-PLAN.md (Core Pages)
</output>
